services:
  dev:
    tty: true
    hostname: devcontainer
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.devcontainer
      pull: true
      args:
        PROJECT_NAME: nyaascripts
    environment:
      SSH_AUTH_SOCK: /tmp/ssh-agent.sock
    volumes:
      # SSH agent forwarding
      - type: bind
        source: /tmp/ssh-agent.sock
        target: /tmp/ssh-agent.sock
        bind:
          create_host_path: false
      # Workspace
      - ..:/workspace/nyaascripts:cached
      # Home
      - ../volumes/home:/home/vscode:cached
    networks:
      default:
      switchboard-network:
        aliases:
          - nyaascripts
networks:
  switchboard-network:
    name: switchboard
